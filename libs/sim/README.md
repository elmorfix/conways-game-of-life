# sim

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build sim` to build the library.

## Running unit tests

Run `nx test sim` to execute the unit tests via [Jest](https://jestjs.io).
